import React from 'react';
import './App.css';

const App = () => {
  return (
    <>
      <h1 style={{textAlign: 'center'}}>
        ReactJS + TypeScript + GTS (Google TypeScript + ESLint + Prettier) +
        Husky Boilerplate
      </h1>
    </>
  );
};

export default App;
